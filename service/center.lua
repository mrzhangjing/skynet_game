local skynet = require("skynet")

local command = require("command_base")

function command.getRoleBaseInfo(roleId)
    -- local i = 1
    -- for i = 1, 1000000000 do
    --     i = i + 1
    -- end
    return {}
end

skynet.start(
    function()
        print("[Center] √")
    end
)
